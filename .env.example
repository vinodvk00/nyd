# Database
DB_USERNAME=nyduser
DB_PASSWORD=your_secure_password_here
DB_DATABASE=nyd_timetrack

# JWT Secret (generate: openssl rand -hex 32)
JWT_SECRET=your_jwt_secret_here

# Encryption key for API tokens - 64 hex chars (generate: openssl rand -hex 32)
ENCRYPTION_KEY=your_encryption_key_here

# Email (Resend) - for password reset
RESEND_API_KEY=your_resend_api_key_here
FRONTEND_URL=http://your-server-ip

# Frontend API URL (used during build)
NEXT_PUBLIC_API_URL=http://your-server-ip/api
